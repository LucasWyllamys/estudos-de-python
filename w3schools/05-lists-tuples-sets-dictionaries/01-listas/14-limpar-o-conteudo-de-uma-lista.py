# O método clear() esvazia a lista.
# A lista ainda permanece, mas não tem conteúdo.
myList = ['maçã', 'banana', 'morango']
myList.clear()  # Limpa o conteúdo da lista, no entanto, o objeto myList ainda existe!
print(myList)   # Saída: []
